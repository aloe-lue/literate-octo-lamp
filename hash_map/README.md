# hashmap

hashmap here in this case is only using string worst case scenario for hashmap to run is O(n) if the implementation for the hash function is either modulo or multiplication, could use perfect hash in the imperfect world but consequently is deep bounded theory could've done so much research into the rabbit hole. 
