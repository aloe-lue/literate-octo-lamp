# insertion-sort

separate sorted values and unsorted value left and right respectively
compare the prev and curr then shifts them, when in the middle it shifts values until
all values in sorted values are well sorted.
