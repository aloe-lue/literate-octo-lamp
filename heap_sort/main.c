#include <stdio.h>
#include <math.h>

int main() 
{
        /*
         * PI should be so precise that the cartesian points at the exact place
         * never
         * 
         */
        int broo = 1;
        const long double PI = acos(-1);
        if (PI) {
                printf("%Lf\n", PI); 
        }
        if (broo) broo = 0;

        return 0;
}
