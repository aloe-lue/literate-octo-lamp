# binary-search-tree

bst when build is balanced binary search tree but when numbers of insertion gets
unbalanced so I created rebalance that does the job although should be way more
efficient.  I have not used hash for removing duplicates since it creates
segmentation fault i thought it was slow pretty weird right here.
