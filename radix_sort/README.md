# radix-sort

radix sort uses radix array 2d to store radix indexed value where the value is equal to decimal 0-9 non negative value
relative to the radix index so it can be brought back to array(not actually) and stable.
