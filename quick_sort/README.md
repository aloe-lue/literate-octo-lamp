# quick-sort

It uses partition as it's utility function that needs array, lo and hi  in order to sort array back to front or front to back using comparison and swapping.
to use the partition to sort the quicksort it should be implemented in recursion so that it calls itself then calling partition until it's sorted.
